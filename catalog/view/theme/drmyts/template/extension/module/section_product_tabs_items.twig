{% import "drmyts/template/macro/product-card.twig" as macros %}

{% for product in products %}

    {{ macros.productCard(
        product.thumb, 
        product.product_id, 
        product.name, 
        product.manufacturer, 
        product.brand_href, 
        product.price, 
        product.special, 
        product.discount, 
        product.in_wishlist, 
        product.href, 
        'splide__slide product-cats__product-slide'
    ) }}

{% endfor %}